<Page
    x:Class="CityApp.Views.Account"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CityApp.Views"
    xmlns:dm="using:CityApp.DataModel"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">

            <Grid x:Name="LoginGrid" Width="300" Height="200">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>

                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Username" Margin="10" 
                   HorizontalAlignment="Left" VerticalAlignment="Center" />
                <TextBox Grid.Row="0" Grid.Column="1" Margin="10" Width="150" x:Name="Username" />
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Password" Margin="10" 
                   HorizontalAlignment="Left" VerticalAlignment="Center" />
                <TextBox Grid.Row="1" Grid.Column="1" Margin="10" Width="150" x:Name="Password" />
                <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" TextAlignment="Right" Text="{Binding ErrorText, Mode=OneWay}" Foreground="Red"/>
                <Button Grid.Row="3" Grid.Column="0" Content="Log In" Command="{Binding LogInCommand}">
                    <Button.CommandParameter>
                        <dm:LogInCredentials u="{x:Bind Username.Text, Mode=OneWay}" p="{x:Bind Password.Text, Mode=OneWay}"/>
                    </Button.CommandParameter>
                </Button>
                <Button Grid.Row="3" Grid.Column="1" Content="Register" HorizontalAlignment="Right" Command="{Binding RegisterCommand}"/>
            </Grid>
            <Button Content="Continue As A Guest"/>
        </StackPanel>
    </Grid>
</Page>
